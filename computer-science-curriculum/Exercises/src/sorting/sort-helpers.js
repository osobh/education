'use strict'

function swap(arr, idx1, idx2) {
  var temp = arr[idx1];
    arr[idx1] = arr[idx2];
    arr[idx2] = temp;
}

function merge(arr1, arr2) {

}

function partition(arr, left, right) {

}

module.exports = {
  swap,
  merge,
  partition
}
